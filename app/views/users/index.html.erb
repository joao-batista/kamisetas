<br/>
<% @users.each do |user| %>
  <div class="col-sm-3">
    <div class="thumbnail">
      <%= link_to(image_tag(user.picture.thumb.url), user) %>
      <h4><%= user.location %></h4>
      <p><%= link_to 'Edit', edit_user_path(user) %></p>
    </div>
  </div>
<% end %>
